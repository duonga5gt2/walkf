export const formDataCalculation = (title, values, results) => {
    return {title,
            values,
            results, logo: "walk-outline"}
}

export const formFoodLogHistory = (name, title, food) => {
    

    return {name, title, food, logo: "fast-food-outline"}
}